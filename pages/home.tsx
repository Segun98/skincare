import { HomeUI } from "@/components/Home";
import { Layout } from "@/components/Layout";
import React from "react";

const Home = () => {
  return (
    <Layout>
      <HomeUI />
    </Layout>
  );
};

export default Home;
